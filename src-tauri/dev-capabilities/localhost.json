{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dev-localhost",
  "description": "Loopback HTTP access for the local dev Worker. Added at runtime by lib.rs under #[cfg(debug_assertions)] and NEVER compiled into a release build — see the comment there. This file deliberately lives outside src-tauri/capabilities/, because everything in that directory is embedded unconditionally.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "http:default",
      "allow": [
        { "url": "http://localhost:*/*" },
        { "url": "http://127.0.0.1:*/*" }
      ]
    }
  ]
}
